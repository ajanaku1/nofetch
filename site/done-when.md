# NoFetch landing page: done when

- `node --test site/landing.test.mjs` exits 0.
- `site/index.html`, `site/styles.css`, and `site/script.js` load without browser console errors.
- The page renders at 1440×1000 and 390×844 without horizontal overflow, clipped primary content, or overlapping controls.
- Primary navigation, GitHub links, internal anchors, and the copy-command button are usable by keyboard.
- The page uses the approved Local Return logo and accurately describes NoFetch as CPU-only, local-first, reversible, and fetch-gated.
- The proof section contains the repository-backed metrics 21.62 tok/s, 1.87 GB peak memory, and 0.66 ARC-Easy acc_norm.
- Reduced-motion styling is present.
